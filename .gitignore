node_modules
dist
coverage
*.local
.DS_Store

# Original asset-pack source (Rive/Flutter origin) — sorted copies live in public/assets/.
pomodoro_bird_animation_assets/

# Environment files and credentials — never commit secrets (rules.md §6).
.env
.env.*
# .env.example is committed deliberately: variable NAMES ONLY, empty values.
!.env.example
credentials/*.json
